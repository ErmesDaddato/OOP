**Cerca i file all'interno di una directory** (di default è la corrente)

```bash
find -name file.txt 
```

Per cercare un file con solo la sua estensione, è possibile usare la wildcard * 

```bash
find -name *.txt 
```


#Ricerca_File #Linux #Comandi